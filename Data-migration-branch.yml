name: Data Migration

on:
  push:
    branches:
      - dev
      - test
      - stage
      - prod
  pull_request:
    branches:
      - main  # Ensure merging into main doesn't trigger deployments

jobs:
  copy-composer-dag:
    runs-on: ubuntu-latest
    permissions:
      id-token: 'write'
      contents: 'read'

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine Environment
        run: |
          case "${{ github.ref_name }}" in
            "dev")
              echo "PROJECT_ID=prj-shrd-dev-67236" >> $GITHUB_ENV
              echo "SERVICE_ACCOUNT=sa-composer-us-4001@prj-shrd-dev-67236.iam.gserviceaccount.com" >> $GITHUB_ENV
              echo "WORKLOAD_IDENTITY_PROVIDER=projects/1046068350740/locations/global/workloadIdentityPools/wif-pool/providers/wif-provider" >> $GITHUB_ENV
              echo "GCS_BUCKET=gs://gcs-duse4usw1-shrd-artifact-dev-67236/test_folder/" >> $GITHUB_ENV
              echo "ENVIRONMENT=dev" >> $GITHUB_ENV
              ;;
            "test")
              echo "PROJECT_ID=prj-test-123456" >> $GITHUB_ENV
              echo "SERVICE_ACCOUNT=sa-composer-test@prj-test-123456.iam.gserviceaccount.com" >> $GITHUB_ENV
              echo "WORKLOAD_IDENTITY_PROVIDER=projects/123456789/locations/global/workloadIdentityPools/wif-test/providers/wif-provider" >> $GITHUB_ENV
              echo "GCS_BUCKET=gs://test-composer-bucket/dags/" >> $GITHUB_ENV
              echo "ENVIRONMENT=test" >> $GITHUB_ENV
              ;;
            "stage")
              echo "PROJECT_ID=prj-stage-123456" >> $GITHUB_ENV
              echo "SERVICE_ACCOUNT=sa-composer-stage@prj-stage-123456.iam.gserviceaccount.com" >> $GITHUB_ENV
              echo "WORKLOAD_IDENTITY_PROVIDER=projects/123456789/locations/global/workloadIdentityPools/wif-stage/providers/wif-provider" >> $GITHUB_ENV
              echo "GCS_BUCKET=gs://stage-composer-bucket/dags/" >> $GITHUB_ENV
              echo "ENVIRONMENT=stage" >> $GITHUB_ENV
              ;;
            "prod")
              echo "PROJECT_ID=prj-prod-123456" >> $GITHUB_ENV
              echo "SERVICE_ACCOUNT=sa-composer-prod@prj-prod-123456.iam.gserviceaccount.com" >> $GITHUB_ENV
              echo "WORKLOAD_IDENTITY_PROVIDER=projects/123456789/locations/global/workloadIdentityPools/wif-prod/providers/wif-provider" >> $GITHUB_ENV
              echo "GCS_BUCKET=gs://prod-composer-bucket/dags/" >> $GITHUB_ENV
              echo "ENVIRONMENT=prod" >> $GITHUB_ENV
              ;;
            "main")
              echo "Final code storage—No deployment." && exit 0
              ;;
            *)
              echo "Branch not configured for deployment." && exit 1
              ;;
          esac

      - name: Google Authentication
        id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: "${{ env.PROJECT_ID }}"
          service_account: "${{ env.SERVICE_ACCOUNT }}"
          workload_identity_provider: "${{ env.WORKLOAD_IDENTITY_PROVIDER }}"

      - name: Sync DAGs (Python)
        run: gsutil -m rsync -r -d dags/ "${{ env.GCS_BUCKET }}"

      - name: Sync SQL folder
        run: gsutil -m rsync -r -d dags/sql/ "${{ env.GCS_BUCKET }}"

      - name: Sync DAG Config
        run: gsutil -m rsync -r -d dags/dag_config/${{ env.ENVIRONMENT }}/ "${{ env.GCS_BUCKET }}"
